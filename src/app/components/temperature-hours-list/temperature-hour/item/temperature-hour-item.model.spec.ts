import { TemperatureHourItem } from './temperature-hour-item.model';

describe('TemperatureHourListItem', () => {
  it('should create an instance', () => {
    expect(new TemperatureHourItem()).toBeTruthy();
  });
});

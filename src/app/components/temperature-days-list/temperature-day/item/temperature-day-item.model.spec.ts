import { TemperatureDayItem } from './temperature-day-item.model';

describe('TemperatureDayItem', () => {
  it('should create an instance', () => {
    expect(new TemperatureDayItem()).toBeTruthy();
  });
});

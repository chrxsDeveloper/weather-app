import { TemperatureHourListItem } from './temperature-hour-list-item.model';

describe('TemperatureHourListItem', () => {
  it('should create an instance', () => {
    expect(new TemperatureHourListItem()).toBeTruthy();
  });
});
